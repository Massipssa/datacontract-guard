<!-- copied from docs/naming-conventions.md -->

# Naming Conventions

Guidelines for dataset and column naming:

- Use `snake_case` for column names.
- Prefix IDs with the entity name, e.g., `customer_id`.
- Use clear date column names like `created_at`, `updated_at`.
- Avoid ambiguous abbreviations and prefer explicit names.
